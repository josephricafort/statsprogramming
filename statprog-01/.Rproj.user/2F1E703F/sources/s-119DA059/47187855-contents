my_list <- list(
  c("first", "element"),
  c(TRUE, FALSE, TRUE),
  list(),
  1:20
)

my_list
str(my_list)

my_data <- data.frame(
  name = c('Marc', 'Manel', 'Christine', 'Sonia'),
  sex = c('male', 'male', 'female', 'female'),
  age = c(40, 45, 30, 43))
my_data
is.list(my_data)
